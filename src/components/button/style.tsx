import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  Normalbutton: {
    width: 390,
    height: 88,
    backgroundColor: '#FFF7DA',
    borderWidth: 20,
    borderColor: '#FFF7DA',
    borderRadius: 45,
  },
});
