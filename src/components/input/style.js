import { StyleSheet } from 'react-native';
import { primaryFontSize, primaryFontWeight } from '../../style/share';
export const styles = StyleSheet.create({
  NormalInput: {
    width: 376,
    height: 81,
    fontWeight: primaryFontWeight,
    fontSize: primaryFontSize,
    borderColor: '#C4C4C4',
    borderWidth: 2,
    borderRadius: 11,
    textAlign: 'center',
  },
});
